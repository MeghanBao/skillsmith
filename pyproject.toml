[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "skillsmith"
version = "0.1.0"
description = "Forge, test, and ship AI agent skills from raw company knowledge — SOPs, docs, chat logs — with a generate→test→eval→iterate loop and multi-format export."
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
authors = [{ name = "Meghan Bao" }]
keywords = ["claude", "agents", "skills", "sop", "knowledge-distillation", "anthropic"]
dependencies = [
    "anthropic>=0.40.0",
    "pydantic>=2.6",
    "pyyaml>=6.0",
    "click>=8.1",
    "rich>=13.7",
    "pypdf>=4.0",          # PDF source loader
    "beautifulsoup4>=4.12", # HTML source loader
]

[project.optional-dependencies]
web = [
    "fastapi>=0.110",
    "uvicorn[standard]>=0.29",
    "python-multipart>=0.0.9",
]
dev = [
    "pytest>=8.0",
    "pytest-cov>=4.1",
    "ruff>=0.4",
    "fastapi>=0.110",
    "python-multipart>=0.0.9",
    "httpx>=0.27",
]

[project.scripts]
skillsmith = "skillsmith.cli:main"

[project.urls]
Homepage = "https://github.com/MeghanBao/skillsmith"
Repository = "https://github.com/MeghanBao/skillsmith"

[tool.hatchling.build.targets.wheel]
packages = ["src/skillsmith"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint.per-file-ignores]
# FastAPI dependency-injection idiom: File()/Body() in argument defaults.
"src/skillsmith/web/app.py" = ["B008"]
